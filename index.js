const animal = "dog"

function myAnimal() {
  return animal
}

function yourAnimal() {
  const animal = "cat"
  return animal
}

const two = 2

function add2(n) {
  return n + two

  // Feel free to move things around!
  
}